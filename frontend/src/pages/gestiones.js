import React from 'react';
import Layout from '../components/Layout';

const GestionesPage = () => {
  return (
    <Layout>
      {/* Contenido de la página de gestión de bases */}
      <h1>Gestión de bases</h1>
      <p>Bienvenido a la página de gestión de bases</p>
    </Layout>
  );
};

export default GestionesPage;